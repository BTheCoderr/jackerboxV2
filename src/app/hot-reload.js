// This file helps improve hot reloading in Next.js
// It's a no-op file that gets imported in key components

export default function setupHotReload() {
  // This is intentionally empty
  // The file's existence and import is what matters for hot reloading
  return null;
}

// Add a comment to force a change when needed
// Last updated: ${new Date().toISOString()} 